# === Server B (this machine): Qwen2.5-Coder + orchestrator + sandbox ===

# Base URL of the Ollama OpenAI-compatible API endpoint.
OLLAMA_BASE_URL=http://localhost:11434/v1

# Model to use for the agent. Must support OpenAI-style tool/function calling.
# qwen2.5-coder-16k is qwen2.5-coder:14b with num_ctx baked in via
# ollama/Modelfile (see the setup step below and OLLAMA_NUM_CTX further
# down for why a plain model tag isn't enough).
MODEL_NAME=qwen2.5-coder-16k

# === Server A: Qwen3 + RAG + eval-formatting (separate machine once it exists) ===
#
# Server B always calls out to Server A - Server A never calls back. Until
# real Server A hardware is set up, leave SERVER_A_BASE_URL pointing at
# localhost: every call then fails fast (connection refused) and Server B
# transparently falls back to its own local test-writer / skips eval
# formatting - see README's "Two-machine deployment" section.
SERVER_A_BASE_URL=http://localhost:8000

# Shared secret sent as the X-API-Key header on every call to Server A - must
# be set to the SAME value in Server A's own .env (SERVER_A_API_KEY there
# too). Generate one with: openssl rand -hex 32
# Required (server refuses to start without it) once SERVER_A_BASE_URL
# points at anything other than localhost/127.0.0.1.
SERVER_A_API_KEY=

# Not just one inference call - Server A's /generate-requirement internally
# runs up to 6 iterations against qwen3:32b on CPU-only hardware. CPU decode
# is memory-bandwidth bound (~1-2.5 tokens/sec estimated on this hardware for
# a ~19-20GB Q4_K_M model), so a single iteration can take several minutes
# before counting prefill on the growing 16384-token context. 3600s (1hr)
# budgets for 6 such iterations with margin - still an estimate, not a
# measurement; verify against real Server A hardware. Must stay comfortably
# under ENDPOINT_TIMEOUT below - re-tune both together.
SERVER_A_REQUEST_TIMEOUT=3600

# Maximum number of agent-loop iterations before giving up. Not every
# iteration makes progress (malformed JSON, a rejected filename, etc. waste
# some), so this is set higher than the number of real attempts you'd expect
# to need.
MAX_ITERATIONS=16
# Iteration budget for refining an existing session (smaller task, smaller budget).
REFINE_MAX_ITERATIONS=10

# Context window (tokens) requested from Ollama per call. Ollama's own
# default (4096) is much smaller than qwen2.5-coder:14b's actual max (32768)
# and can be too small for a long multi-iteration agent loop - once exceeded,
# Ollama silently truncates the oldest messages instead of erroring, which
# can look like the model "forgetting" earlier context. Uses more VRAM the
# higher it's set.
OLLAMA_NUM_CTX=16384

# Directory (relative or absolute) where per-session code is written.
WORKSPACE_ROOT=workspace

# Directory where a JSON run log is written per /generate-code request.
LOGS_DIR=logs

# Timeouts, in seconds.
WEB_SEARCH_TIMEOUT=10
TEST_RUN_TIMEOUT=60
# Wraps the entire /generate-code (or /refine) call: phase 1 (Server A, up to
# SERVER_A_REQUEST_TIMEOUT above) plus phase 2 (this machine's own
# GPU-backed agent loop, up to MAX_ITERATIONS). 480s was calibrated for
# phase 2 alone, before Server A's CPU-only phase 1 was added in front of
# it - now needs to cover both: SERVER_A_REQUEST_TIMEOUT (3600s) + phase 2's
# original 480s + margin.
ENDPOINT_TIMEOUT=4200

# The OpenAI SDK requires a non-empty API key even though Ollama ignores it.
OPENAI_API_KEY=ollama

# Tavily API key for web_search (get one at tavily.com). Optional - if unset,
# web_search automatically falls back to DuckDuckGo (no key needed).
TAVILY_API_KEY=

# run_tests executes model-controlled shell commands, so by default it runs
# them inside an isolated Docker sandbox (see docker/sandbox.Dockerfile) -
# no network, memory/CPU/process limits, read-only filesystem outside the
# workspace mount. Requires building the image once:
#   docker build -f docker/sandbox.Dockerfile -t agentic-sandbox:latest .
# Set to false only if Docker isn't available (commands then run directly on
# the host shell - understand the command-injection risk before doing that).
SANDBOX_ENABLED=true
SANDBOX_IMAGE=agentic-sandbox:latest
SANDBOX_MEMORY_LIMIT=256m
SANDBOX_CPU_LIMIT=0.5
SANDBOX_PIDS_LIMIT=128

# === Server A's own vars (only read by server_a/main.py) ===
#
# These only matter on the machine actually running `uvicorn
# server_a.main:app` - irrelevant to Server B's own process, but kept in the
# same example file since it's convenient to copy this whole file to Server
# A's checkout and just fill in its section.

# Server A's own local Ollama instance serving the big reasoning model
# (Qwen3). Real Server A hardware is CPU-only (Xeon Silver 4208, 16 threads,
# 32GB RAM, no GPU). qwen3:32b (dense, Q4_K_M) chosen per explicit preference
# for accuracy over speed - the largest dense Qwen3 size that still fits in
# 32GB RAM. Estimated breakdown (not yet measured on real hardware - verify
# with `ollama ps` once Server A is up): ~19-20GB model weights + ~3-6GB KV
# cache at SERVER_A_MODEL_NUM_CTX=16384 (larger than an 8B model's cache at
# the same context, since KV-cache size scales with layer count/width, not
# parameter count alone) + ~2-3GB OS/Docker sandbox overhead => ~24-29GB
# total, ~3-8GB headroom. RAM is tight and this is an estimate - if real
# usage shows OOM/swapping, drop to qwen3:14b (~8-9GB) instead.
# SERVER_A_MODEL_NAME must be the tag derived via `ollama create
# qwen3-32b-16k -f ollama/Modelfile.qwen3`, not the plain `qwen3:32b` pull -
# see that Modelfile for why (otherwise SERVER_A_MODEL_NUM_CTX below is
# silently ignored, same issue as MODEL_NAME/OLLAMA_NUM_CTX above).
SERVER_A_MODEL_BASE_URL=http://localhost:11434/v1
SERVER_A_MODEL_NAME=qwen3-32b-16k
# Matches Server B's OLLAMA_NUM_CTX - correctness (not losing/truncating
# context) takes priority over the extra CPU latency a larger context costs.
SERVER_A_MODEL_NUM_CTX=16384
SERVER_A_MODEL_API_KEY=ollama
